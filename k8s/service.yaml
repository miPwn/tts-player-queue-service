apiVersion: v1
kind: Service
metadata:
  name: tts-playback-service
  namespace: tts-production
  labels:
    app: tts-playback
spec:
  type: ClusterIP
  selector:
    app: tts-playback
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
